# Requirements für die Zuordnungen Lern-App
# Python 3.6 oder höher erforderlich

# ===== WEB-VERSION (empfohlen) =====
# Für die Browser-Version (web_app.py / START.py):
flask>=2.0.0

# ===== DESKTOP-VERSION (optional) =====
# Für die Desktop-Version (zuordnungen_app.py):
# tkinter ist normalerweise in Python enthalten
# Falls nicht, installiere es mit:
# Ubuntu/Debian: sudo apt-get install python3-tk
# Fedora: sudo dnf install python3-tkinter
# macOS: tkinter ist standardmäßig enthalten
# Windows: tkinter ist standardmäßig enthalten

# ===== Installation =====
# Web-Version: pip install flask
# Desktop-Version: Keine Installation nötig (tkinter)
